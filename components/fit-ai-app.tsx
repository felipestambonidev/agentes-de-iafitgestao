'use client'

import { useMemo, useState } from 'react'
import ReactMarkdown from 'react-markdown'
import remarkGfm from 'remark-gfm'
import { Prism as SyntaxHighlighter } from 'react-syntax-highlighter'
import { oneDark } from 'react-syntax-highlighter/dist/esm/styles/prism'
import { ArrowUp, ChevronDown, CircleHelp, Code2, FileText, FolderKanban, Home, MessageSquare, MoreHorizontal, Paperclip, Plus, Search, Settings, Sparkles, UserRound, X } from 'lucide-react'

const services = ['Fluig', 'Protheus', 'Service Desk', 'Marketing', 'Comercial', 'DHO', 'COT', 'Diretoria']
const initialMessages = [
  { role: 'assistant', content: 'Olá, Felipe! Sou a **FIT AI**, como posso te ajudar hoje?\n\nPosso apoiar você com processos, documentos, análises e dúvidas dos departamentos.' },
]

function MarkdownMessage({ content }: { content: string }) {
  return <ReactMarkdown remarkPlugins={[remarkGfm]} components={{ code({ className, children, ...props }) {
    const match = /language-(\w+)/.exec(className || '')
    return match ? <SyntaxHighlighter style={oneDark} language={match[1]} PreTag="div" className="!my-3 !rounded-xl !bg-[#111923] !p-4 text-[13px]" {...props}>{String(children).replace(/\n$/, '')}</SyntaxHighlighter> : <code className="rounded bg-secondary px-1.5 py-0.5 font-mono text-[12px] text-cyan-300" {...props}>{children}</code>
  }}}>{content}</ReactMarkdown>
}

export default function FitAiApp() {
  const [loggedIn, setLoggedIn] = useState(false)
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [activeService, setActiveService] = useState('Fluig')
  const [expanded, setExpanded] = useState(true)
  const [input, setInput] = useState('')
  const [messages, setMessages] = useState(initialMessages)
  const [mobileSidebar, setMobileSidebar] = useState(false)
  const sessionId = useMemo(() => 'fit-ai-demo-session', [])

  if (!loggedIn) return <main className="flex min-h-screen items-center justify-center bg-background px-6 py-12"><div className="w-full max-w-[400px]">
    <div className="mb-12 flex items-center gap-3"><div className="flex size-11 items-center justify-center rounded-2xl bg-primary text-primary-foreground"><Sparkles size={21} /></div><span className="text-xl font-semibold tracking-tight">FIT AI</span></div>
    <p className="mb-2 font-mono text-xs uppercase tracking-[0.24em] text-muted-foreground">Bem-vindo de volta</p><h1 className="mb-3 text-3xl font-semibold tracking-tight">Entre na sua conta</h1><p className="mb-9 text-sm leading-6 text-muted-foreground">Acesse seus agentes e continue sua jornada de produtividade.</p>
    <form onSubmit={(e) => { e.preventDefault(); setLoggedIn(true) }} className="space-y-5"><label className="block text-sm font-medium">E-mail<input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="seu@email.com" className="mt-2 h-12 w-full rounded-xl border bg-card px-4 outline-none transition focus:border-primary focus:ring-2 focus:ring-primary/20" /></label><label className="block text-sm font-medium">Senha<input required type="password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="••••••••" className="mt-2 h-12 w-full rounded-xl border bg-card px-4 outline-none transition focus:border-primary focus:ring-2 focus:ring-primary/20" /></label><button className="h-12 w-full rounded-xl bg-primary font-semibold text-primary-foreground transition hover:opacity-90">Entrar</button></form>
    <p className="mt-8 text-center text-xs text-muted-foreground">Acesso seguro para sua equipe</p>
  </div></main>

  const submit = async () => { const text = input.trim(); if (!text) return; setMessages((current) => [...current, { role: 'user', content: text }]); setInput(''); try { const response = await fetch('/api/chat', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ session_id: sessionId, agent_type: activeService, user_message: text }) }); const data = await response.json(); setMessages((current) => [...current, { role: 'assistant', content: data.message || 'Recebi sua mensagem. Como posso continuar ajudando?' }]) } catch { setMessages((current) => [...current, { role: 'assistant', content: 'Sua mensagem foi registrada. O agente estará disponível assim que o webhook for conectado.' }]) } }

  return <main className="flex min-h-screen bg-background text-foreground"><aside className={`${mobileSidebar ? 'translate-x-0' : '-translate-x-full'} fixed inset-y-0 left-0 z-30 flex w-[280px] flex-col border-r bg-sidebar p-5 transition-transform md:static md:translate-x-0`}><div className="mb-9 flex items-center justify-between"><div className="flex items-center gap-2.5"><div className="flex size-9 items-center justify-center rounded-xl bg-primary text-primary-foreground"><Sparkles size={17} /></div><span className="font-semibold tracking-tight">FIT AI</span></div><button className="md:hidden" onClick={() => setMobileSidebar(false)} aria-label="Fechar menu"><X size={18} /></button></div><button onClick={() => setMessages(initialMessages)} className="mb-7 flex h-11 items-center justify-center gap-2 rounded-xl border border-dashed border-muted-foreground/30 text-sm font-medium transition hover:border-primary hover:text-primary"><Plus size={17} /> Nova conversa</button><nav className="space-y-1"><p className="mb-3 px-3 font-mono text-[10px] uppercase tracking-[0.18em] text-muted-foreground">Workspace</p>{[['Home', Home], ['Projetos', FolderKanban], ['Chats', MessageSquare]].map(([name, Icon]) => <button key={name as string} className={`flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm ${name === 'Chats' ? 'bg-secondary font-medium' : 'text-muted-foreground hover:bg-secondary/70 hover:text-foreground'}`}><Icon size={17} />{name as string}</button>)}</nav><div className="mt-8"><button onClick={() => setExpanded(!expanded)} className="flex w-full items-center justify-between px-3 text-left font-mono text-[10px] uppercase tracking-[0.18em] text-muted-foreground"><span>Área de Serviços</span><ChevronDown size={14} className={expanded ? '' : '-rotate-90'} /></button>{expanded && <div className="mt-3 space-y-0.5">{services.map((service) => <button key={service} onClick={() => setActiveService(service)} className={`flex w-full items-center rounded-lg px-3 py-2 text-sm transition ${activeService === service ? 'bg-primary/10 font-medium text-primary' : 'text-muted-foreground hover:bg-secondary hover:text-foreground'}`}>{service}</button>)}</div>}</div><div className="mt-auto border-t pt-4"><button className="flex w-full items-center gap-3 rounded-lg px-2 py-2 text-sm text-muted-foreground hover:text-foreground"><Settings size={17} /> Configurações</button><div className="mt-4 flex items-center gap-3 rounded-xl bg-secondary/60 p-3"><div className="flex size-8 items-center justify-center rounded-full bg-primary text-xs font-semibold text-primary-foreground">FS</div><div className="min-w-0"><p className="truncate text-sm font-medium">Felipe Stamboni</p><p className="truncate text-xs text-muted-foreground">felipe@empresa.com</p></div><MoreHorizontal size={17} className="ml-auto text-muted-foreground" /></div></div></aside><div className="flex min-w-0 flex-1 flex-col"><header className="flex h-[72px] items-center justify-between border-b px-5 md:px-8"><div className="flex items-center gap-3"><button className="md:hidden" onClick={() => setMobileSidebar(true)} aria-label="Abrir menu"><MoreHorizontal size={20} /></button><div><p className="text-sm font-semibold">{activeService}</p><p className="text-xs text-muted-foreground">Agente especializado</p></div></div><div className="flex items-center gap-2"><button className="hidden rounded-lg p-2 text-muted-foreground hover:bg-secondary hover:text-foreground sm:block" aria-label="Buscar"><Search size={18} /></button><button className="rounded-lg p-2 text-muted-foreground hover:bg-secondary hover:text-foreground" aria-label="Ajuda"><CircleHelp size={18} /></button></div></header><section className="flex flex-1 flex-col overflow-y-auto"><div className="mx-auto flex w-full max-w-3xl flex-1 flex-col gap-7 px-5 py-10 md:px-8 md:py-14">{messages.map((message, index) => <div key={index} className={`flex gap-3 ${message.role === 'user' ? 'justify-end' : ''}`}><div className={`flex size-8 shrink-0 items-center justify-center rounded-xl ${message.role === 'assistant' ? 'bg-primary text-primary-foreground' : 'bg-secondary text-muted-foreground'}`}>{message.role === 'assistant' ? <Sparkles size={15} /> : <UserRound size={15} />}</div><div className={`max-w-[85%] text-sm leading-7 ${message.role === 'user' ? 'rounded-2xl rounded-tr-sm bg-primary px-4 py-2 text-primary-foreground' : 'pt-0 text-foreground'}`}><MarkdownMessage content={message.content} /></div></div>)}</div><div className="mx-auto w-full max-w-3xl px-5 pb-6 md:px-8"><div className="rounded-2xl border bg-card p-2 shadow-sm"><textarea value={input} onChange={(e) => setInput(e.target.value)} onKeyDown={(e) => { if (e.key === 'Enter' && !e.shiftKey && !e.nativeEvent.isComposing && e.keyCode !== 229) { e.preventDefault(); submit() } }} placeholder={`Pergunte algo para o agente de ${activeService}...`} rows={2} className="w-full resize-none bg-transparent px-3 py-2 text-sm leading-6 outline-none placeholder:text-muted-foreground" /><div className="flex items-center justify-between px-2"><div className="flex gap-1"><button className="rounded-lg p-2 text-muted-foreground hover:bg-secondary hover:text-foreground" aria-label="Anexar arquivo"><Paperclip size={17} /></button><button className="rounded-lg p-2 text-muted-foreground hover:bg-secondary hover:text-foreground" aria-label="Inserir código"><Code2 size={17} /></button><button className="rounded-lg p-2 text-muted-foreground hover:bg-secondary hover:text-foreground" aria-label="Inserir documento"><FileText size={17} /></button></div><button onClick={submit} disabled={!input.trim()} className="flex size-9 items-center justify-center rounded-xl bg-primary text-primary-foreground transition hover:opacity-90 disabled:cursor-not-allowed disabled:opacity-40" aria-label="Enviar mensagem"><ArrowUp size={18} /></button></div></div><p className="mt-3 text-center text-[11px] text-muted-foreground">A FIT AI pode cometer erros. Verifique informações importantes.</p></div></section></div></main>
}
