'use client'
import useSWR from 'swr'
import { useState } from 'react'
import Link from 'next/link'
const fetcher = (url:string) => fetch(url).then(r=>r.json())
type User={id:string;email:string;name:string;role:string;active:boolean}
export default function AdminUsers(){const {data,mutate}=useSWR<{users:User[]}>('/api/admin/users',fetcher);const [form,setForm]=useState({name:'',email:'',password:'',role:'user'});async function create(e:React.FormEvent){e.preventDefault();await fetch('/api/admin/users',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});setForm({name:'',email:'',password:'',role:'user'});mutate()}async function remove(id:string){if(confirm('Excluir este usuário?')){await fetch('/api/admin/users',{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});mutate()}}return <main className="min-h-screen bg-background p-8 text-foreground"><div className="mx-auto max-w-5xl"><p className="font-mono text-xs uppercase tracking-widest text-muted-foreground">FIT AI / Administração</p><div className="mt-3 flex items-center justify-between gap-4"><h1 className="text-3xl font-semibold">Usuários e funções</h1><Link href="/" className="rounded-lg border px-4 py-2 text-sm font-medium transition hover:bg-secondary">Voltar para a plataforma</Link></div><form onSubmit={create} className="mt-8 grid gap-3 rounded-2xl border bg-card p-5 md:grid-cols-5"><input required placeholder="Nome" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="rounded-lg border bg-background px-3 py-2"/><input required type="email" placeholder="E-mail" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} className="rounded-lg border bg-background px-3 py-2"/><input required minLength={8} type="password" placeholder="Senha" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} className="rounded-lg border bg-background px-3 py-2"/><select value={form.role} onChange={e=>setForm({...form,role:e.target.value})} className="rounded-lg border bg-background px-3 py-2"><option value="user">Usuário</option><option value="manager">Gestor</option><option value="admin">Administrador</option></select><button className="rounded-lg bg-primary px-4 py-2 font-semibold text-primary-foreground">Criar usuário</button></form><div className="mt-8 overflow-hidden rounded-2xl border bg-card"><div className="grid grid-cols-5 gap-4 border-b p-4 text-xs font-semibold uppercase text-muted-foreground"><span>Nome</span><span className="col-span-2">E-mail</span><span>Função</span><span>Ação</span></div>{data?.users?.map(u=><div key={u.id} className="grid grid-cols-5 items-center gap-4 border-b p-4 text-sm last:border-0"><span>{u.name}</span><span className="col-span-2">{u.email}</span><span>{u.role}</span><button onClick={()=>remove(u.id)} className="text-left text-destructive">Excluir</button></div>)}</div></div></main>}
